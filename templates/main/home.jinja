{% extends "main/base.jinja" %}

{% block content %}
    <section class="pt-32 pb-20 px-6 hero-shell">
        <div class="container mx-auto grid lg:grid-cols-2 gap-12 items-center">
            <div>
                <div class="kicker mb-5">{{ site.hero_badge_year }} · {{ site.hero_badge_text }}</div>
                <h1 class="display text-5xl md:text-6xl lg:text-7xl leading-tight">
                    {{ site.hero_heading_line_one }} <span class="accent-text">{{ site.hero_heading_highlight_one }}</span><br>
                    {{ site.hero_heading_line_two }} <span class="accent-text">{{ site.hero_heading_highlight_two }}</span>
                </h1>
                <p class="text-muted text-lg md:text-xl mt-6 max-w-xl">
                    {{ site.hero_subtitle }}
                </p>
                <div class="mt-10 flex flex-wrap gap-4">
                    <button type="button" data-modal-open="contact-modal" class="btn-primary">
                        {{ site.primary_cta_label }}
                    </button>
                    <a href="{{ site.secondary_cta_url }}" class="btn-outline">{{ site.secondary_cta_label }}</a>
                </div>
                <div class="mt-10 flex flex-wrap gap-6 text-xs uppercase tracking-[0.3em] text-muted">
                    <span>Hızlı kurulum</span>
                    <span>Ölçeklenebilir altyapı</span>
                    <span>Şeffaf raporlama</span>
                </div>
            </div>
            <div class="panel rounded-[32px] p-8">
                <div class="kicker">Performans Panosu</div>
                <div class="grid grid-cols-2 gap-6 mt-6">
                    {% for stat in stats %}
                    <div class="rounded-2xl border border-white/10 p-4 bg-white/10">
                        <div class="text-3xl font-semibold {{ stat.color_class }}">{{ stat.value }}</div>
                        <div class="text-xs uppercase tracking-[0.25em] text-muted mt-2">{{ stat.label }}</div>
                    </div>
                    {% endfor %}
                </div>
                <div class="mt-8 rounded-2xl border border-white/10 p-5 text-sm text-muted">
                    Haftalık büyüme takibi, kullanıcı aktivasyonu ve gelir metrikleri tek panelde.
                </div>
            </div>
        </div>
    </section>

    <section class="py-20 px-6">
        <div class="container mx-auto">
            <div class="flex flex-col items-center text-center mb-14">
                <div class="kicker mb-4">ÇALIŞMA SÜRECİMİZ</div>
                <h2 class="display text-4xl md:text-5xl">Bütünsel Yaklaşım</h2>
            </div>
            <div class="grid lg:grid-cols-3 gap-8">
                <div class="panel rounded-[28px] p-8">
                    <div class="kicker mb-4 text-accent">01. Strateji</div>
                    <h3 class="text-xl font-semibold mb-3">SaaS Büyüme Planı</h3>
                    <p class="text-muted">Fiyatlandırma, aktivasyon ve elde tutma metriklerini netleştiriyoruz.</p>
                </div>
                <div class="panel rounded-[28px] p-8">
                    <div class="kicker mb-4 text-accent">02. Mimari</div>
                    <h3 class="text-xl font-semibold mb-3">Ölçeklenebilir Altyapı</h3>
                    <p class="text-muted">Güvenlik, performans ve multi-tenant yapıların tamamını kuruyoruz.</p>
                </div>
                <div class="panel rounded-[28px] p-8">
                    <div class="kicker mb-4 text-accent">03. Operasyon</div>
                    <h3 class="text-xl font-semibold mb-3">Satış ve Destek Akışı</h3>
                    <p class="text-muted">CRM, müşteri başarı ve destek süreçlerini otomasyonla bağlıyoruz.</p>
                </div>
            </div>
        </div>
    </section>
    <section id="services" class="py-20 px-6 section-wash">
        <div class="container mx-auto">
            <div class="flex flex-col items-center text-center mb-14">
                <div class="kicker mb-4">Çözümler</div>
                <h2 class="display text-4xl md:text-5xl">{{ site.services_title }}</h2>
            </div>

            <div class="grid lg:grid-cols-3 gap-8">
                {% for service in services %}
                <div class="panel p-10 rounded-[36px] group">
                    <div class="w-14 h-14 rounded-2xl flex items-center justify-center mb-6 bg-red-50">
                        <i class="{{ service.icon_class }} text-2xl text-accent"></i>
                    </div>
                    <h3 class="text-2xl font-semibold mb-4">{{ service.title }}</h3>
                    <p class="text-muted leading-relaxed mb-6">{{ service.description }}</p>
                    <ul class="text-sm space-y-3 text-muted">
                        <li><i class="fa-solid fa-check text-accent mr-2"></i> {{ service.bullet_one }}</li>
                        <li><i class="fa-solid fa-check text-accent mr-2"></i> {{ service.bullet_two }}</li>
                    </ul>
                </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <section id="portfolio" class="py-20 px-6">
        <div class="container mx-auto">
            <div class="flex flex-col items-center text-center mb-14">
                <div class="kicker mb-4">REFERANSLAR</div>
                <h2 class="display text-4xl md:text-5xl">{{ site.portfolio_title }}</h2>
            </div>

            <div class="grid md:grid-cols-3 gap-10">
                {% for item in portfolio_items %}
                <a href="{{ item.get_absolute_url() }}" class="panel rounded-[28px] overflow-hidden group">
                    <img src="{{ item.image_url }}" alt="{{ item.title }}" class="w-full h-56 object-cover">
                    <div class="p-8">
                        <h3 class="text-2xl font-semibold mb-3">{{ item.title }}</h3>
                        <p class="text-muted line-clamp-3 whitespace-pre-line">{{ item.description }}</p>
                        <div class="mt-6 text-xs uppercase tracking-[0.3em] text-accent">Detayı gör</div>
                    </div>
                </a>
                {% endfor %}
            </div>
        </div>
    </section>

    <section id="contact" class="py-20 px-6 section-wash">
        <div class="container mx-auto max-w-5xl panel rounded-[40px] p-12 md:p-16 text-center">
            <div class="kicker mb-4">İş Birliği</div>
            <h2 class="display text-4xl md:text-5xl mb-6">{{ site.contact_title|linebreaksbr }}</h2>
            <p class="text-muted text-lg mb-10 max-w-2xl mx-auto">{{ site.contact_body }}</p>
            <button type="button" data-modal-open="contact-modal" class="btn-primary">
                {{ site.contact_button_label }}
                <i class="fa-solid fa-arrow-right"></i>
            </button>
        </div>
    </section>
{% endblock %}
